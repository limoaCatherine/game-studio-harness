# 自述

本文说明这套平台**是干什么的、为什么这样拆、值不值得上**。部署步骤看 [README.md](README.md)，外接软件看 [docs/工具/总览.md](docs/工具/总览.md)。

## 用途

给已经用 Cursor（或同类 AI 编程工具）做游戏的团队，提供一套**可搬家的制作操作系统**：

- 用规则管「永远要遵守的事」，不用每轮重说。
- 用技能管「这件事怎么做」，一件事一份正文。
- 用职种管「这个岗位的主路径」，点名只打开第一步。
- 用项目库管「当前做到哪、正式面在哪、上次验证过什么」，下一手不靠聊天续命。

它**不是**引擎、**不是**数值表、**不是**美术管线软件。那些仍用你现有的 Unity / Unreal / Excel / DCC。本仓只部署「代理怎么工作」的文件。

## 目的

1. **换人、换会话、换机器仍能接着做。** 现行卡和正式面地图写在业务根，不写在某次对话里。
2. **控制上下文。** 开场只注入点名正文，而不是把一百多个技能全部塞进窗口。
3. **控制写权限。** 默认写隔离根；晋升须人准，且只回写记录集，避免代理把正式表整本覆盖。
4. **控制外接。** 每日必用的核心直连，其余懒接，避免 Unity 空等把 Excel 一起堵住。
5. **可对外复制。** 去掉项目名、本机路径和密钥后，其他工作室可以一键落到自己的 Cursor。

## 它在解决什么曲线

下面三张图是**架构对比模型**，用来说明取舍，不是某工作室的 profiler 截图。

### 1. 上下文占用

全量灌入时，技能 + 职种 + 外接工具表会随轮次把窗口顶满，后面只能靠遗忘。点名注入把增长率压在还能工作的区间。

![上下文占用](docs/图表/上下文占用.svg)

### 2. 冷启动时延

若 30 多个 MCP 启动全部拉起，发现阶段会被空等宿主和大工具表拖到几十秒。核心直连、其余懒接，开场只付「能握手」的代价。

![冷启动时延](docs/图表/冷启动时延.svg)

### 3. 正式面误写风险

多个会话并行时，直接写正式面的风险接近叠加。默认进沙盒、人准后只合记录格，风险曲线接近持平。

![正式面风险](docs/图表/正式面风险.svg)

## 优劣势

### 优势

| 点 | 说明 |
|---|---|
| 可部署、可审计 | 规则、技能、职种、脚本都在文件里，能 diff、能验收，不靠口头习惯。 |
| 上下文可预期 | 名单只决定开场注入，不是运行时防火墙；缺的技能过程中再打开。 |
| 写隔离是默认 | 表 / 代码 / 引擎试验 / 草稿走同一张正式面地图，晋升口径统一。 |
| 外接可降级 | 无软件机也能把架构落地。MCP 红灯不等于架构失败。 |
| 岗位可拆 | 职种不预展开路径；职责不同时按职种开子代理，主会话收口。 |
| 去敏可公开 | 本仓不含业务与密钥，适合作为模板分发。 |

### 劣势

| 点 | 说明 |
|---|---|
| 前置成本 | 要会写加载计划、改 `surfaces.json`，第一周比「直接跟代理聊天」重。 |
| 仍依赖 Cursor | 钩子、规则、MCP 发现绑在 Cursor 用户目录。换编辑器要改接线。 |
| 软件要另装 | 一键部署不装 Excel / Unity / DCC。外接要按 [工具总览](docs/工具/总览.md) 自己备宿主。 |
| 不是知识图谱 | 第四层是档案柜加流水。不会自动推理「上次为什么那样定」。 |
| 封版约束 | 改四层结构必须转向、改计划、写决策，不能沿旧名单加第五层。 |
| 示意不是 SLA | 上文曲线是模型。你机器上的秒数取决于宿主和网络。 |
| 中文路径与脚本 | 技能和脚本大量中文文件名，在非 UTF-8 终端里要小心编码。 |

### 什么时候不该上

- 只要一次性改几行代码，没有持续制作会话。
- 团队不接受「先定档再写」的纪律。
- 需要一个开箱即用的引擎或数值中台——那是另一类产品。

## 和「只丢一堆 Skills」的差别

```mermaid
flowchart TB
  subgraph dump [全量灌入]
    A1[规则+全部技能+全部 MCP] --> A2[窗口顶满]
    A2 --> A3[遗忘 / 串岗 / 误写正式面]
  end
  subgraph named [本架构]
    B1[L1 宪法始终在] --> B2[L2 点名写计划]
    B2 --> B3[只注入本轮正文]
    B3 --> B4[默认写隔离根]
    B4 --> B5[人准后回写记录集]
  end
```

## 推荐阅读序

1. 本文：用途与取舍  
2. [docs/架构.md](docs/架构.md)：目录与四层对照  
3. [docs/部署指南.md](docs/部署指南.md)：一键与失败表  
4. [docs/工具/总览.md](docs/工具/总览.md)：要装哪些软件  
5. 打开业务根，写第一份 `loadplan.json`
