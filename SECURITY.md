# 安全

## 报告

请用 GitHub 私密漏洞报告，不要开公开 issue。

## 范围

本仓、安装器、五套工具目录里的架构文件。

## 密钥

- 密钥不进仓库。`mcp.json.example` 只放占位符。
- 安装器不会覆盖已有的 `mcp.json`。
- 若密钥误提交：立即轮换，再重写历史，不要只 revert。

## 供应链

- 发布与安装只指向本仓。
- 第三方包未列入官方面之前按非官方处理。
