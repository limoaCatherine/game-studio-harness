# Kimi Code

原生入口：`AGENTS.md`、`rules/gsh.md`。技能落到 `~/.kimi-code/skills`。职种在 `agents/`。

没有事件钩子。`HOOKS.md` 补开场与关项。业务根另有 `.kimi-code/AGENTS.md`。

```bash
python -m gsh setup --tools kimi --profile core --workspace /path/to/studio --yes
```
