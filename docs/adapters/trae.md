# Trae

原生入口：`AGENTS.md`、`rules/gsh.md`。Trae 读 `.trae/rules`。技能/职种按 profile 投影。

没有事件钩子。`HOOKS.md` 补开场与关项。业务根另有 `.trae/rules/gsh.md`。

```bash
python -m gsh setup --tools trae --profile core --workspace /path/to/studio --yes
```
