# Qwen Code

原生入口：`QWEN.md`、`AGENTS.md`、`rules/gsh.md`。技能落到 `~/.qwen/skills`。职种在 `agents/`。

没有事件钩子。`HOOKS.md` 补开场与关项。业务根另有 `QWEN.md` 与 `.qwen/QWEN.md`。

```bash
python -m gsh setup --tools qwen --profile core --workspace /path/to/studio --yes
```
