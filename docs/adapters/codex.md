# Codex

Codex 读 `AGENTS.md`。技能投影到 `~/.codex/skills` 与 `~/.agents/skills`。职种在 `~/.codex/agents`。

Codex 没有事件钩子运行时。家目录带 `HOOKS.md`：开场自己打开 `current.md`，关项自己核 `verify-report.json`。

仓库根 `.codex/AGENTS.md` 与根 `AGENTS.md` 同源。

```bash
python -m gsh setup --tools codex --profile full --workspace /path/to/studio --yes
```
