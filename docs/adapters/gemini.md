# Gemini CLI

原生入口：`GEMINI.md`、`rules/gsh.md`。技能落到 `~/.gemini/skills`。职种在 `agents/`。

没有事件钩子。`HOOKS.md` 补开场与关项。业务根另有 `GEMINI.md`。

```bash
python -m gsh setup --tools gemini --profile core --workspace /path/to/studio --yes
```
