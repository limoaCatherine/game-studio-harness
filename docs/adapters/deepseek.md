# DeepSeek

原生入口：`AGENTS.md`、`rules/gsh.md`。技能投影到 `~/.dsh/skills` 与 `~/.agents/skills`。职种在 `~/.dsh/agents`。

没有事件钩子。`HOOKS.md` 补开场与关项。

```bash
python -m gsh setup --tools deepseek --profile full --workspace /path/to/studio --yes
```
