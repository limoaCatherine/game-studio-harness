{
  "title": "多工具架构一键部署",
  "software_in_pack": false,
  "tools": ["cursor", "claude", "codex", "grok", "deepseek"],
  "includes": ["rules", "skills", "mcp-logic", "crafts", "hooks", "harness-scripts", "adapters", "workspace-scaffold", "docs"],
  "excludes": ["dcc", "engines", "mcp-backends", "venvs", "secrets", "business-tables"],
  "entry": "一键部署.bat",
  "steps": [
    {"id": "read", "do": "读 README.md 与 自述.md", "required": true},
    {"id": "install", "do": "一键部署.bat 或 python install.py --workspace <workspace> --tools all", "required": true},
    {"id": "verify", "do": "python verify_install.py --workspace <workspace> --tools all", "pass": "ok architecture install", "required": true},
    {"id": "surfaces", "do": "改 <workspace>/.harness/surfaces.json", "required": true},
    {"id": "first-session", "do": "写 loadplan.json 后跑 ~/.gsh/harness/scripts/生成会话能力名单.py", "required": true},
    {"id": "tools", "do": "按需读 docs/工具/总览.md 再装宿主", "required": false}
  ]
}
