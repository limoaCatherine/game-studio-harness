# /route-task

Thin slash wrapper. Open the source skill:

- `skills/route-task/SKILL.md`

Do not invent ids. After writing `.harness/sessions/<id>/loadplan.json`, run:

```text
python harness/scripts/生成会话能力名单.py <id>
```
